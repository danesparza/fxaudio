#!/bin/sh

# There are a few special things we need to do to use ALSA
# The user we create has to be in the 'audio' group in order to use ALSA devices

# Create the user fxaudio if it doesn't already exist:
sudo id -u fxaudio >/dev/null 2>&1 || sudo useradd fxaudio

# Add fxaudio to the audio group
sudo usermod -aG audio fxaudio